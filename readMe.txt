from command line currently it only does one html file at a time instead of whole directory of html files 

you would use regular commands with flags but when you add the corpusDir flag, you would put just one html file and not the whole working directory

